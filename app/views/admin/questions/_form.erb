<%= form_with model: [:admin, @test, @question] do |form| %>

  <%= render 'shared/form_errors', resource: @question %>
  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
<% end %>

<p> <%= link_to 'To test', admin_test_path(@question.test) %></p>