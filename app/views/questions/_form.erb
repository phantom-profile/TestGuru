<%= form_with model: [@test, @question] do |form| %>

  <%= render 'shared/form_errors', resource: @question %>
  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
<% end %>

<p> <%= link_to 'To test', test_path(@question.test) %></p>